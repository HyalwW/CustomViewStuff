<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.customviewstuff">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MyTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".activities.JumpBeanActivity"></activity>
        <activity android:name=".activities.TriAngleActivity" />
        <activity android:name=".activities.SAEActivity" />
        <activity android:name=".activities.CartActivity" />
        <activity android:name=".activities.RippleActivity" />
        <activity android:name=".activities.ArrowActivity" />
        <activity android:name=".activities.HeartActivity" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>